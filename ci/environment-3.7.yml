name: dask-kubernetes-test
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.7
  - nomkl
  - pip
  - dask=2
  # testing / CI
  - flake8
  - ipywidgets
  - pytest
  - black
  - pyyaml
  - pip:
    - kubernetes==9
